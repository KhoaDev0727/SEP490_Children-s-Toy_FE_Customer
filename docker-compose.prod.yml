version: "3.8"

services:
  fe-customer:
    build: .
    container_name: toystore-fe-customer
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - PORT=3000
    ports:
      - "3000:3000"
    restart: unless-stopped
